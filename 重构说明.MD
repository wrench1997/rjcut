# 代码重构说明

## 概述

本次重构完成了三个主要任务：

1. ✅ **引入 pipeline.mode 字段** - 支持纯场景模式，通过 if/else 在 task_runner.py 和 lip_sync.py 绕过 Whisper，用纯 ffmpeg 组装画面
2. ✅ **拆分 task_runner.py** - 利用注册表模式将 agent_draft、compose、chanjing_video 拆分到不同的文件中
3. ✅ **封装音视频处理组件** - 将下载、ASR 识别、FFMPEG 切割、OSS 上传等动作封装成独立的 Class，使用责任链模式进行调用

---

## 1. pipeline.mode 纯场景模式

### 功能说明

新增 `pipeline.mode` 参数，支持两种运行模式：

- **`normal`** (默认): 正常模式，使用 Whisper 重新识别字幕并烧录
- **`scene_only`**: 纯场景模式，跳过 Whisper ASR 识别，使用提供的 subtitle_json 直接烧录字幕

### 使用方式

```json
{
  "pipeline": {
    "mode": "scene_only",
    "use_transitions": false,
    "transition_type": "fade",
    "transition_duration": 0.8
  },
  "input": {
    "subtitle_json_url": "http://example.com/subtitle.json"
  }
}
```

### 修改的文件

- **lip_sync.py**: `compose_from_timeline()` 函数新增 `mode` 和 `subtitle_json` 参数
- **tasks/compose_from_draft.py**: 支持下载 subtitle_json 并传递给 lip_sync
- **service_runner.py**: 支持纯场景模式参数

### 代码示例

```python
# lip_sync.py 中的核心逻辑
if mode == "scene_only":
    # 纯场景模式 - 跳过 Whisper，使用提供的 subtitle_json
    if not subtitle_json or not os.path.isfile(subtitle_json):
        raise ValueError("纯场景模式需要提供有效的 subtitle_json 路径")
    
    # 直接使用提供的 JSON 烧录字幕，不重新识别
    burn_whisper_subtitle(
        input_video=tmp_merged,
        output_video=tmp_subtitled,
        json_path=tmp_subtitle_json.name,
        # ... 其他参数
    )

elif resync:
    # 正常模式 - 重新识别并烧录字幕
    resync_subtitle(
        input_video=tmp_merged,
        output_video=tmp_subtitled,
        model_size=model_size,
        device=device,
        # ... 其他参数
    )
```

---

## 2. task_runner.py 拆包 - 注册表模式

### 架构设计

使用**注册表模式**将不同类型的任务处理逻辑拆分到独立文件：

```
tasks/
├── __init__.py              # 任务注册表
├── components.py            # 音视频处理组件（责任链）
├── agent_draft.py           # 草稿模式任务处理器
├── agent_compose.py         # 完整合成任务处理器
├── compose_from_draft.py    # 从草稿合成任务处理器
└── chanjing_video.py        # 蝉镜数字人任务处理器
```

### 注册表实现

```python
# tasks/__init__.py
TASK_HANDLERS: Dict[str, Callable] = {}

def register_task(task_type: str):
    """装饰器：注册任务处理器"""
    def decorator(func: Callable):
        TASK_HANDLERS[task_type] = func
        return func
    return decorator

def get_task_handler(task_type: str) -> Callable:
    """获取任务处理器"""
    handler = TASK_HANDLERS.get(task_type)
    if not handler:
        raise ValueError(f"未知的任务类型：{task_type}")
    return handler
```

### 使用示例

```python
# tasks/agent_draft.py
from tasks import register_task

@register_task("agent_draft")
def run_agent_draft_task(task_id: str, payload: dict, trace_id: str, merchant_id: str):
    # 任务处理逻辑
    pass
```

```python
# task_runner.py
from tasks import get_task_handler

def run_task(task_id: str, task_type: str, payload: dict, trace_id: str, merchant_id: str):
    """根据任务类型分发到对应的处理器"""
    handler = get_task_handler(task_type)
    handler(task_id, payload, trace_id, merchant_id)
```

### 支持的任务类型

- `agent_draft`: 草稿模式 - 切割视频并生成可编辑脚本
- `agent_compose`: 完整合成模式 - 切割 + 合成 + 字幕烧录
- `compose_from_draft`: 从草稿合成 - 基于草稿生成最终视频
- `dh_generate_video`: 蝉镜数字人视频生成
- `dh_create_person`: 蝉镜自定义数字人训练

---

## 3. 音视频处理组件 - 责任链模式

### 组件设计

将音视频处理的动作封装成独立的 Component 类：

```python
# tasks/components.py

class Component(ABC):
    """组件基类"""
    def __init__(self, name: str):
        self.name = name
        self._next: Optional['Component'] = None
    
    def set_next(self, component: 'Component') -> 'Component':
        """设置下一个组件"""
        self._next = component
        return component
    
    def handle(self, context: 'TaskContext') -> 'TaskContext':
        """处理请求并传递给下一个组件"""
        context = self.process(context)
        if self._next:
            return self._next.handle(context)
        return context
    
    @abstractmethod
    def process(self, context: 'TaskContext') -> 'TaskContext':
        """具体处理逻辑，由子类实现"""
        pass
```

### 任务上下文

```python
class TaskContext:
    """任务上下文，在组件链中传递"""
    
    def __init__(self, task_id: str, payload: dict, merchant_id: str, task_dir: str):
        self.task_id = task_id
        self.payload = payload
        self.merchant_id = merchant_id
        self.task_dir = task_dir
        self.input_dir = os.path.join(task_dir, "input")
        self.output_dir = os.path.join(task_dir, "output")
        self.scene_dir = os.path.join(task_dir, "scenes")
        
        # 中间产物
        self.input_video_path: Optional[str] = None
        self.script_path: Optional[str] = None
        self.script_data: Optional[dict] = None
        self.corrections_path: Optional[str] = None
        self.font_path: Optional[str] = None
        self.bgm_path: Optional[str] = None
        
        # 结果数据
        self.result: Dict[str, Any] = {}
        self.error: Optional[str] = None
```

### 可用组件

| 组件类 | 功能 |
|--------|------|
| `DownloadInputComponent` | 下载主视频文件 |
| `DownloadScriptComponent` | 下载脚本文件 |
| `DownloadCorrectionsComponent` | 下载纠错字典 |
| `DownloadFontComponent` | 下载字体文件 |
| `DownloadBgmComponent` | 下载背景音乐 |
| `DownloadScenesComponent` | 下载场景素材 |
| `UploadFileComponent` | 上传文件到 OSS |

### 构建责任链

```python
def build_download_chain() -> Component:
    """构建完整的下载责任链"""
    download_input = DownloadInputComponent()
    download_script = DownloadScriptComponent()
    download_corrections = DownloadCorrectionsComponent()
    download_font = DownloadFontComponent()
    download_bgm = DownloadBgmComponent()
    download_scenes = DownloadScenesComponent()
    
    # 按顺序连接
    download_input.set_next(download_script) \
                  .set_next(download_corrections) \
                  .set_next(download_font) \
                  .set_next(download_bgm) \
                  .set_next(download_scenes)
    
    return download_input
```

### 使用示例

```python
# 在任务处理器中使用责任链
from tasks.components import TaskContext, build_download_chain

# 创建上下文
context = TaskContext(task_id, payload, merchant_id, task_dir)

# 执行下载责任链
download_chain = build_download_chain()
download_chain.handle(context)

# 现在 context 中包含了所有下载的文件路径
print(f"主视频：{context.input_video_path}")
print(f"脚本：{context.script_path}")
print(f"字体：{context.font_path}")
```

---

## 重构优势

### 1. 模块化
- 每个任务类型独立文件，职责清晰
- 组件可复用，易于测试和维护

### 2. 可扩展性
- 新增任务类型只需添加新文件并使用 `@register_task` 装饰器
- 新增组件只需继承 `Component` 基类并实现 `process()` 方法

### 3. 灵活性
- `pipeline.mode` 支持快速切换不同处理模式
- 责任链可动态组合，适应不同业务场景

### 4. 代码质量
- 消除了 task_runner.py 的代码臃肿问题（从 1000+ 行拆分）
- 每个组件职责单一，符合 SRP 原则

---

## 迁移指南

### 旧代码
```python
# task_runner.py (旧)
from task_runner import run_agent_draft_task

run_agent_draft_task(task_id, payload, trace_id, merchant_id)
```

### 新代码
```python
# 方式 1: 使用统一入口
from task_runner import run_task

run_task(task_id, "agent_draft", payload, trace_id, merchant_id)

# 方式 2: 直接调用处理器 (向后兼容)
from tasks.agent_draft import run_agent_draft_task

run_agent_draft_task(task_id, payload, trace_id, merchant_id)
```

---

## 测试建议

### 1. 纯场景模式测试
```bash
# 测试 scene_only 模式
curl -X POST http://localhost:8000/v1/tasks \
  -H "Content-Type: application/json" \
  -d '{
    "type": "compose_from_draft",
    "pipeline": {
      "mode": "scene_only"
    },
    "input": {
      "subtitle_json_url": "http://example.com/subtitle.json"
    }
  }'
```

### 2. 正常模式测试
```bash
# 测试 normal 模式 (默认)
curl -X POST http://localhost:8000/v1/tasks \
  -H "Content-Type: application/json" \
  -d '{
    "type": "agent_compose",
    "pipeline": {
      "mode": "normal"
    }
  }'
```

---

## 注意事项

1. **向后兼容**: 所有旧的任务处理函数都保留了包装器，确保现有代码不受影响
2. **默认行为**: `pipeline.mode` 默认为 `"normal"`，保持原有行为不变
3. **错误处理**: 纯场景模式下如果缺少 `subtitle_json_url` 会抛出明确的错误提示
4. **组件顺序**: 责任链中组件的执行顺序很重要，请根据业务逻辑合理排列

---

## 后续优化建议

1. **配置化责任链**: 通过配置文件动态组装责任链
2. **组件池**: 实现组件复用，减少对象创建开销
3. **异步支持**: 将组件改为异步版本，提高并发性能
4. **监控埋点**: 在每个组件中添加性能监控和日志埋点
5. **熔断机制**: 为组件添加熔断器，防止级联失败

---

## 相关文件清单

### 新增文件
- `tasks/__init__.py` - 任务注册表
- `tasks/components.py` - 音视频处理组件
- `tasks/agent_draft.py` - 草稿模式处理器
- `tasks/agent_compose.py` - 完整合成处理器
- `tasks/compose_from_draft.py` - 从草稿合成处理器
- `tasks/chanjing_video.py` - 蝉镜数字人处理器

### 修改文件
- `task_runner.py` - 改为使用注册表模式
- `lip_sync.py` - 新增 pipeline.mode 支持
- `service_runner.py` - 新增 pipeline.mode 支持

---

**重构完成时间**: 2026-04-10  
**重构版本**: v2.0
