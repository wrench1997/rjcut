# 字幕精确坐标控制功能说明

## 📋 改动总结

本次更新为字幕系统添加了**像素级精确坐标控制**功能，解决了传统 `alignment + margin` 定位方式颗粒度过粗的问题。

---

## 🎯 核心改进

### 1. 新增参数

在 `subtitle_effects.py` 的以下函数中添加了精确坐标参数：

#### `generate_word_ass()`
```python
def generate_word_ass(
    segments: List[dict],
    output_path: str,
    # ... 其他参数 ...
    
    # === 新增：精确坐标控制 ===
    position_x: Optional[int] = None,   # 精确 X 坐标（像素）
    position_y: Optional[int] = None,   # 精确 Y 坐标（像素）
    use_relative_pos: bool = False,     # 是否使用相对坐标 (0-1)
) -> str:
```

#### `burn_whisper_subtitle()`
```python
def burn_whisper_subtitle(
    input_video: str,
    output_video: str,
    json_path: str,
    # ... 其他参数 ...
    
    # === 新增：精确坐标控制 ===
    position_x: Optional[int] = None,   # 精确 X 坐标（像素）
    position_y: Optional[int] = None,   # 精确 Y 坐标（像素）
    use_relative_pos: bool = False,     # 是否使用相对坐标 (0-1)
) -> str:
```

### 2. 特效函数更新

所有内部特效函数都已更新支持坐标参数：
- `_eff_karaoke()` - 卡拉 OK 填充特效
- `_eff_highlight()` - 逐字高亮特效
- `_eff_typewriter()` - 打字机特效
- `_eff_bounce()` - 弹跳出现特效
- `_eff_ad()` - 带货广告风特效

### 3. 技术实现

使用 ASS 字幕的 `\pos(x,y)` 标签实现精确控制：

```python
# 构建坐标前缀
pos_prefix = ""
if pos_x is not None and pos_y is not None:
    pos_prefix = "{\\pos(" + str(pos_x) + "," + str(pos_y) + ")}"

# 添加到字幕文本中
text = pos_prefix + text
```

---

## 📖 使用方式

### 方式 1：绝对像素坐标

直接指定字幕的像素坐标位置：

```python
from subtitle_effects import burn_whisper_subtitle

burn_whisper_subtitle(
    input_video="input.mp4",
    output_video="output.mp4",
    json_path="whisper.json",
    effect="ad",
    
    # 精确坐标控制（1080x1920 竖屏分辨率下）
    position_x=540,   # 水平居中
    position_y=1500,  # 垂直 1500px 位置（偏下，适合字幕）
)
```

### 方式 2：相对坐标（推荐）

使用 0-1 之间的小数，自动适配不同分辨率：

```python
burn_whisper_subtitle(
    input_video="input.mp4",
    output_video="output.mp4",
    json_path="whisper.json",
    effect="highlight",
    
    # 相对坐标模式
    use_relative_pos=True,
    position_x=0.5,   # 水平 50% 位置（居中）
    position_y=0.75,  # 垂直 75% 位置
)
```

### 方式 3：Shell 脚本配置

在 `run_draft_full.sh` 中通过环境变量配置：

```bash
# 绝对像素坐标
export SUBTITLE_POSITION_X=960
export SUBTITLE_POSITION_Y=800
export SUBTITLE_USE_RELATIVE_POS=false

# 或相对坐标
export SUBTITLE_POSITION_X=0.5
export SUBTITLE_POSITION_Y=0.75
export SUBTITLE_USE_RELATIVE_POS=true

./run_draft_full.sh
```

---

## 🎨 常见位置预设（竖屏 1080x1920）

| 位置描述 | position_x | position_y | 绝对坐标示例 | 适用场景 |
|---------|-----------|-----------|-------------|---------|
| 顶部居中 | 0.5 | 0.1 | (540, 192) | 标题、品牌名 |
| 中央居中 | 0.5 | 0.5 | (540, 960) | 强调文字、关键信息 |
| 底部居中 | 0.5 | 0.75 | (540, 1440) | 常规字幕（推荐） |
| 底部居中 | 0.5 | 0.8 | (540, 1536) | 常规字幕（偏下） |
| 左下角 | 0.1 | 0.75 | (108, 1440) | 角标、注释 |
| 右下角 | 0.9 | 0.75 | (972, 1440) | 水印、LOGO |

---

## ⚙️ 参数优先级

当同时设置多种定位参数时，优先级如下：

```
精确坐标 (position_x/y) > 传统参数 (alignment/margin/offset)
```

即：
- 如果设置了 `position_x` 和 `position_y`，则忽略：
  - `alignment`（对齐方式）
  - `margin_v`（垂直边距）
  - `margin_l`（左边距）
  - `margin_r`（右边距）
  - `offset_x`（水平偏移）
  - `offset_y`（垂直偏移）
- 如果未设置精确坐标，则使用传统参数定位

---

## 📐 坐标系统说明

```
(0,0) ┌─────────────────────┐ (1080, 0)
      │                     │
      │                     │
      │                     │
      │     视频画面         │
      │    (竖屏 1080x1920)   │
      │                     │
      │                     │
      │                     │
(0,1920) └─────────────────────┘ (1080, 1920)

示例（1080x1920 竖屏分辨率）：
  - 左上角：x=0, y=0
  - 右上角：x=1080, y=0
  - 左下角：x=0, y=1920
  - 右下角：x=1080, y=1920
  - 正中心：x=540, y=960
```

---

## 📝 API 调用示例

### REST API - 绝对坐标（1080x1920 竖屏）

```bash
curl -X POST "http://your-domain.com:8080/v1/tasks/agent-compose" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "input": {
      "video_url": "https://cdn.example.com/input/video.mp4",
      "script_url": "https://cdn.example.com/input/script.json"
    },
    "subtitle": {
      "effect": "ad",
      "font_size": 88,
      "position_x": 540,
      "position_y": 1500,
      "use_relative_pos": false
    }
  }'
```

### REST API - 相对坐标

```bash
curl -X POST "http://your-domain.com:8080/v1/tasks/agent-compose" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "input": {
      "video_url": "https://cdn.example.com/input/video.mp4",
      "script_url": "https://cdn.example.com/input/script.json"
    },
    "subtitle": {
      "effect": "highlight",
      "font_size": 72,
      "position_x": 0.5,
      "position_y": 0.7,
      "use_relative_pos": true
    }
  }'
```

---

## 🔧 修改文件清单

| 文件 | 改动内容 |
|------|---------|
| `subtitle_effects.py` | 添加精确坐标参数到所有相关函数 |
| `run_draft_full.sh` | 添加环境变量配置和 API 调用参数 |
| `README.MD` | 更新 API 文档，添加第 9 章详细说明 |
| `字幕精确坐标控制说明.MD` | 新建详细说明文档 |

---

## ⚠️ 注意事项

1. **坐标原点**：视频左上角为 `(0, 0)`，向右为 X 正方向，向下为 Y 正方向
2. **分辨率适配**：强烈建议使用相对坐标，可自动适配不同分辨率的视频
3. **字幕边界**：确保坐标不会导致字幕超出画面范围
4. **多行字幕**：当前坐标指字幕区域的基准位置，多行会向下延伸
5. **字体大小影响**：字幕实际占用空间受 `font_size` 影响，需预留足够边距

---

## 🚀 后续扩展建议

如需更高级的坐标控制，可考虑以下扩展：

1. **逐字位置映射** - 为每个字指定独立的 `(x, y)` 坐标，实现弧形、波浪等特效
2. **动态轨迹动画** - 字幕随时间移动的动画路径（如从左飞到右）
3. **旋转角度控制** - 支持字幕旋转一定角度
4. **多区域字幕** - 同时显示多个不同位置的字幕区域

---

## 📞 技术支持

如有问题或建议，请联系开发团队。
