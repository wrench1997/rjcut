# 文件管理规范

本文档说明 RJCut 项目的统一文件管理规范和使用方法。

## 📁 目录结构

```
rjcut/
├── service_data/          # 任务临时数据目录（持久化）
│   └── {task_id}/         # 每个任务的独立工作目录
│       ├── input/         # 输入文件（视频、脚本等）
│       ├── output/        # 输出产物（切片、合成视频等）
│       └── scenes/        # 场景素材
├── fonts/                 # 字体文件目录
└── tasks/                 # 任务管理代码
    ├── components.py      # 统一文件管理组件
    ├── agent_draft.py     # 草稿模式任务
    └── agent_compose.py   # 完整合成任务
```

## 🔧 统一文件管理组件

### 使用方式

```python
from tasks.components import FileManagerComponent

# 创建文件管理器实例
file_manager = FileManagerComponent()
```

### 核心功能

#### 1. 上传任务产物

```python
output_paths = {
    "final_video": "/path/to/final.mp4",
    "timeline_json": "/path/to/timeline.json",
    "transcription_json": "/path/to/transcription.json",
}

uploaded_files = file_manager.upload_task_outputs(
    task_id="task_123",
    merchant_id="merchant_456",
    output_paths=output_paths
)
```

#### 2. 上传视频切片

```python
parts = file_manager.upload_parts(
    parts_dir="/path/to/parts",
    task_id="task_123",
    merchant_id="merchant_456"
)
# 返回：{"part_001": oss_entry, "part_002": oss_entry, ...}
```

#### 3. 上传场景素材

```python
scene_assets = file_manager.upload_scene_assets(
    scene_dir="/path/to/scenes",
    task_id="task_123",
    merchant_id="merchant_456"
)
# 使用 MinIO 内部复制，不经过本地下载上传
```

#### 4. 清理任务目录

```python
success = file_manager.cleanup_task_dir(
    task_dir="/path/to/task_dir",
    ignore_errors=True
)
```

#### 5. 将切片信息附加到脚本

```python
editable_script = file_manager.attach_part_files_to_script(
    editable_script=script_dict,
    parts=parts_dict
)
```

## 📦 OSS 文件管理

### 文件存储策略

- **存储路径格式**: `{merchant_id}/tasks/{task_id}/{file_key}.{ext}`
- **场景素材路径**: `{merchant_id}/scenes/{filename}`
- **默认保留天数**: 30 天（可通过 `FILE_STORAGE_DAYS` 配置）
- **单文件最大大小**: 500 MB（可通过 `FILE_MAX_SIZE_MB` 配置）
- **去重策略**: 启用 hash 去重（可通过 `FILE_ENABLE_DEDUPLICATION` 配置）

### 批量操作

```python
from oss import batch_delete_files, get_file_list, get_merchant_storage_usage

# 批量删除文件
result = batch_delete_files(["key1", "key2", "key3"])

# 获取文件列表（分页）
file_list = get_file_list(
    db_session=db,
    merchant_id="merchant_456",
    limit=50,
    offset=0
)

# 获取商户存储使用情况
usage = get_merchant_storage_usage(db, "merchant_456")
# 返回：{"total_files": 100, "total_bytes": ..., "total_mb": ..., "total_gb": ...}
```

### 清理过期文件

```python
from tasks.cleanup_expired_files import cleanup_expired_files_task

# 执行清理任务
result = cleanup_expired_files_task(
    batch_size=100,
    max_iterations=10
)
```

## 🐳 Docker Compose 配置

### 卷挂载说明

| 服务 | 挂载点 | 说明 |
|------|--------|------|
| api | `/opt/rjcut/service_data:/app/service_data` | 任务数据持久化 |
| api | `./fonts:/app/fonts:ro` | 字体文件（只读） |
| worker | `/opt/rjcut/service_data:/app/service_data` | 任务数据持久化 |
| worker | `./fonts:/app/fonts:ro` | 字体文件（只读） |
| task_watchdog | `/opt/rjcut/service_data:/app/service_data` | 任务数据持久化 |
| task_watchdog | `./fonts:/app/fonts:ro` | 字体文件（只读） |

### 环境变量配置

```yaml
# .env 文件
FILE_STORAGE_DAYS=30          # 文件保留天数
FILE_MAX_SIZE_MB=500          # 单文件最大大小 (MB)
FILE_ENABLE_DEDUPLICATION=true # 启用 hash 去重
```

## 📊 文件生命周期

```
创建 → 上传 → 使用 → 过期 → 清理
  ↓      ↓      ↓      ↓      ↓
本地  MinIO  下载    标记   删除
临时  存储   使用    过期   
```

### 最佳实践

1. **及时清理临时文件**: 任务完成后立即清理本地临时目录
2. **使用统一组件**: 所有文件操作使用 `FileManagerComponent`
3. **错误重试**: 上传操作内置 3 次重试机制
4. **日志记录**: 所有文件操作记录详细日志
5. **定期清理**: 设置定时任务清理过期文件

## 🔍 监控和统计

### Grafana 监控

通过 Loki + Promtail + Grafana 监控文件操作日志：

- 文件上传成功率
- 文件清理统计
- 存储空间使用情况

### 查询存储统计

```python
from oss import get_storage_stats

# 全局统计
global_stats = get_storage_stats(db)

# 按商户统计
merchant_stats = get_storage_stats(db, merchant_id="merchant_456")
```

## 📝 迁移指南

### 从旧代码迁移

**旧代码**:
```python
# 分散的文件操作
parts = _build_parts_and_upload(parts_dir, task_id, merchant_id)
scene_assets = _build_scene_assets_and_upload(scene_dir, task_id, merchant_id)
```

**新代码**:
```python
# 统一的文件管理
file_manager = FileManagerComponent()
parts = file_manager.upload_parts(parts_dir, task_id, merchant_id)
scene_assets = file_manager.upload_scene_assets(scene_dir, task_id, merchant_id)
```

### 兼容性

旧的辅助函数（如 `_build_parts_and_upload`）保留作为兼容层，内部调用新的 `FileManagerComponent`。

---

**最后更新**: 2026-04-14  
**版本**: 2.0
