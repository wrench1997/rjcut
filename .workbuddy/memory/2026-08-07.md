# 2026-08-07 工作日志

## Git push 大文件报错处理

- **问题**：push V2 分支时报"文件太大"。定位元凶为 `studio/electron/bin/ffmpeg.exe`（231MB，超 GitHub 100MB 单文件硬限），在未 push 的 commit `ccfc351` 中引入。
- **同时清理**：`studio/public/wasm/ffmpeg-core/ffmpeg-core.wasm`（31MB）与 `ffmpeg-core.js`（均为构建产物，由 `studio/scripts/copy-native-ffmpeg.mjs`、`copy-ffmpeg-core.mjs` 自动恢复，移除 git 追踪安全）。
- **处理方式**：`git rm --cached`（本地文件保留）+ `.gitignore` 新增规则（`studio/electron/bin/*.exe`、`studio/public/wasm/ffmpeg-core/ffmpeg-core.{js,wasm}`）+ `git commit --amend --no-edit` 合并进未 push 的 commit（历史中从未出现大文件）。
- **结果**：push 成功，`7eaadaa..e798748 V2 -> V2`。本地 ffmpeg/wasm 文件保留，不影响构建；`AddSceneVariantsStep.jsx` 工作区修改未动。
- **教训**：ffmpeg 原生二进制与 wasm 核心属构建产物，不应入库；已有 copy 脚本负责恢复。

## studio "AI 分析素材" 报错修复

- **现象**：点"分析素材"后页面崩溃，报 "Application error: a client-side exception has occurred"。
- **根因**：`studio/src/features/template-batch/steps/AddSceneVariantsStep.jsx` 第 5 行只 `import { useState }`，但 `VfsFolderPicker` 组件（第873行）用了 `useEffect` → `ReferenceError` → Next.js 渲染崩溃。对比同目录 `FillTemplateSlotsStep.jsx` 第5行正确导入了 useEffect，确认是遗漏。
- **修复文件**：`AddSceneVariantsStep.jsx`，4 处改动：
  1. 第5行 import 补 `useEffect`（P0，修崩溃）
  2. 两个 catch 块改显示 `error.message` 而非笼统"请稍后重试"
  3. aiSuggestion 渲染加可选链（`files?.length`、`confidence||0`、`files||[]`）防后端字段缺失再次崩溃
  4. `VfsFileBrowser` 渲染期 `if(){loadDirectory().then(setItems)}` 反模式改为 `useEffect(()=>{},[])`
- **配置确认**：`.env` 已设 `GATEWAY_BASE_URL=http://127.0.0.1:8888`（Docker 部署可达，连通性无问题）；后端模型名默认 `Qwen/Qwen3.5-397B-A17B-FP8`（draft_utils.py:1208）疑似笔误，Qwen 无 3.5 系列，若分析仍失败需核对 gateway 实际模型名。
- **链路**：前端按钮 → `aiSuggestSlotFiles`(aiAssistant.js:461) → POST `/v1/ai/analyze-videos` → `analyze_videos`(api_service.py:1202) → `ai_analyze_videos_via_gateway`(draft_utils.py:1138) → POST `{GATEWAY}/v1/chat/completions`。
- **环境**：npm 代理已切 `127.0.0.1:10808`（原 7890 连接被拒）。

## 服务器 192.168.166.151 git pull 冲突分析与整合方案

- **现象**：151 服务器 `/root/workspaces/rjcut`（基线 `7eaadaa`）pull V2 时被本地修改阻塞：12 个已跟踪文件（.env/api_digital_human.py/api_service.py/chanjing_api_v2.py/config.py/docker-compose.yml/draft_utils.py/gateway/app.py/oss.py/schemas.py/studio/docker-compose.yml/tasks/visual_script_editor.py）+ 1 个未跟踪文件 tasks/chanjing_video.py。
- **远程新增**：`7eaadaa..b10df8a`（含 e798748 "1"、b10df8a "123"）。
- **关键结论**：远程已把配置从 `host.docker.internal`/`localhost` 适配为 `192.168.166.151`（MinIO/蝉镜/前端 API 地址），即**远程已包含 151 部署配置**，服务器本地配置修改大概率冗余。
- **配套改动（必须一起更新）**：`oss.py` 删除 `presigned_put_url` + `schemas.py` 删除 `PresignedUploadRequest`/`UploadConfirmRequest` + `tasks/visual_script_editor.py` 移除 `presigned_put_url` 引用 —— 三件套同步移除了 presigned-put 上传方式，若只更新部分会导致引用断裂。
- **其他代码改动**：draft_utils.py 修 suggestions 空数组误报；schemas.py max_chars_per_line 18→15；api_service.py/chanjing_api_v2.py/gateway/app.py/api_digital_human.py 为开发机最新功能；tasks/chanjing_video.py 为全新 608 行蝉镜数字人视频任务处理器。
- **整合方向**：用远程版本覆盖服务器本地修改（备份后），配置类因远程已含 151 适配、代码类因配套改动必须整体更新。
- **方案（待用户在 151 执行）**：备份未跟踪文件→stash 已跟踪本地修改→pull→验证配置（grep 151 地址）→对比 chanjing_video.py。stash 保留可恢复。
- **用户确认意图**：151 是部署服务器、基线 `7eaadaa` 偏旧，用户要直接用远程正式版覆盖（"本地代码老，把正式版更新到本地"）。Windows 开发机已是 `b10df8a` 最新，无需动。
- **最终方案（151 执行，破坏性+备份）**：备份 .env/docker-compose.yml/studio/docker-compose.yml + 移走未跟踪 tasks/chanjing_video.py → `git fetch origin && git reset --hard origin/V2` → grep 验证 151 地址 → `docker compose down && up -d`。reset --hard 丢弃 151 本地修改，因远程已含 151 部署配置可安全覆盖。
