```md
下面给你一套 **完整可跑的草稿模式测试流程**，你可以直接照着测试。

我按你当前服务形态来写，默认：

- API: `http://192.168.166.151:8001`
- 已有可用 `API_KEY`
- MinIO / Redis / Postgres / worker 都已经启动
- 草稿模式代码已经接入
- 草稿结果中已经支持：
  - `editable_script.part_file`
  - `draft.parts`
  - `draft.parts_count`
- 草稿更新接口已经支持：
  - `replace_mode = merge`
  - `replace_mode = replace`

---

# 一、草稿模式能力说明

草稿模式的目标是：

1. 上传原始视频
2. 生成草稿任务
3. 自动完成切分、转写、生成草稿脚本
4. 返回可编辑脚本 `editable_script`
5. 每段脚本带上 `part_file`
6. 返回所有切片文件 `parts`
7. 返回切片数量 `parts_count`
8. 支持人工修稿 / AI 修稿
9. 支持两种更新模式：
   - `merge`：按 segment.id 合并更新
   - `replace`：整份覆盖更新
10. 支持基于草稿重新合成最终视频

---

# 二、先确认服务已启动

## 1. 启动 API

```bash
cd /root/workspaces/rjcut
uvicorn api_service:app --host 0.0.0.0 --port 8001 --reload
```

## 2. 启动 Worker

另一个终端执行：

```bash
cd /root/workspaces/rjcut
python worker.py
```

## 3. 健康检查

```bash
curl -s http://localhost:8001/health | jq .
```

期望返回：

```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "status": "ok"
  },
  "trace_id": null
}
```

---

# 三、准备环境变量

先在 shell 中设置：

```bash
export BASE_URL="http://192.168.166.151:8001"
export API_KEY="替换成你的真实API_KEY"
```

例如：

```bash
export BASE_URL="http://192.168.166.151:8001"
export API_KEY="rjk_xxxxxxxxxxxxxxxxxxxxxxxxx"
```

## 验证商户信息

```bash
curl -s "$BASE_URL/v1/merchant/info" \
  -H "Authorization: Bearer $API_KEY" | jq .
```

正常会返回商户额度、并发限制等信息。

---

# 四、准备测试素材

你至少需要一个测试视频，比如：

```bash
/root/workspaces/rjcut/鹿茸血广告.mp4
```

要求建议：

- mp4 格式
- 时长不要太长，建议 10 秒到 2 分钟内
- 内容里最好有明确口播，便于 ASR 和切片验证

如果你没有这个文件，可以换成你自己的：

```bash
/root/workspaces/rjcut/test.mp4
```

后续命令里把文件名替换掉即可。

---

# 五、上传测试视频

## 1. 创建预签名上传地址

```bash
curl -s -X POST "$BASE_URL/v1/uploads/presign" \
  -H "Authorization: Bearer $API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "filename": "鹿茸血广告.mp4",
    "content_type": "video/mp4",
    "purpose": "input"
  }' | tee /tmp/presign.json | jq .
```

提取变量：

```bash
export UPLOAD_ID=$(jq -r '.data.upload_id' /tmp/presign.json)
export UPLOAD_URL=$(jq -r '.data.upload_url' /tmp/presign.json)
export OSS_KEY=$(jq -r '.data.oss_key' /tmp/presign.json)

echo "$UPLOAD_ID"
echo "$UPLOAD_URL"
echo "$OSS_KEY"
```

## 2. 上传文件到 MinIO

```bash
curl -X PUT "$UPLOAD_URL" \
  -H "Content-Type: video/mp4" \
  --upload-file /root/workspaces/rjcut/鹿茸血广告.mp4
```

## 3. 确认上传完成

```bash
curl -s -X POST "$BASE_URL/v1/uploads/confirm" \
  -H "Authorization: Bearer $API_KEY" \
  -H "Content-Type: application/json" \
  -d "{\"upload_id\":\"$UPLOAD_ID\"}" | jq .
```

成功后会返回：

- `upload_id`
- `oss_key`
- `size`
- `confirmed: true`

---

# 六、发起草稿任务

这是最小草稿测试，不带 script、不带 corrections、不带 scene。

```bash
curl -s -X POST "$BASE_URL/v1/tasks/agent-draft" \
  -H "Authorization: Bearer $API_KEY" \
  -H "Content-Type: application/json" \
  -d "{
    \"input\": {
      \"video_url\": \"$OSS_KEY\"
    },
    \"pipeline\": {
      \"remove_keyword\": \"转场\",
      \"margin\": 0.15,
      \"min_segment_duration\": 0.1
    },
    \"asr\": {
      \"model\": \"large-v3\",
      \"device\": \"cuda\",
      \"language\": \"zh\"
    },
    \"timeout_seconds\": 1800
  }" | tee /tmp/draft_task.json | jq .
```

提取任务 ID：

```bash
export DRAFT_TASK_ID=$(jq -r '.data.task_id' /tmp/draft_task.json)
echo "$DRAFT_TASK_ID"
```

---

# 七、轮询草稿任务状态

## 1. 单次查询

```bash
curl -s "$BASE_URL/v1/tasks/$DRAFT_TASK_ID" \
  -H "Authorization: Bearer $API_KEY" | jq .
```

## 2. 循环查询直到完成

```bash
while true; do
  curl -s "$BASE_URL/v1/tasks/$DRAFT_TASK_ID" \
    -H "Authorization: Bearer $API_KEY" | jq '.data | {task_id,status,progress,stage,error}'
  sleep 5
done
```

成功时类似：

```json
{
  "task_id": "task_xxx",
  "status": "succeeded",
  "progress": 100,
  "stage": "finished",
  "error": null
}
```

---

# 八、查看草稿详情

```bash
curl -s "$BASE_URL/v1/drafts/$DRAFT_TASK_ID" \
  -H "Authorization: Bearer $API_KEY" | tee /tmp/draft_detail.json | jq .
```

---

# 九、重点检查草稿详情字段

## 1. 查看 `editable_script`

```bash
jq '.data.editable_script' /tmp/draft_detail.json
```

期望类似：

```json
{
  "segments": [
    {
      "id": 1,
      "text": "大家好今天给大家介绍一款蓝牙耳机",
      "type": "human",
      "scene_file": null,
      "part_file": "part_001.mp4",
      "start": 0.0,
      "end": 4.2,
      "duration": 4.2
    },
    {
      "id": 2,
      "text": "它佩戴舒适而且续航时间很长",
      "type": "human",
      "scene_file": null,
      "part_file": "part_002.mp4",
      "start": 4.2,
      "end": 8.6,
      "duration": 4.4
    }
  ]
}
```

重点检查：

- `text` 是否正常
- `part_file` 是否存在
- `start/end/duration` 是否合理
- `part_file` 是否和分段顺序一致

---

## 2. 查看 `parts`

```bash
jq '.data.parts' /tmp/draft_detail.json
```

期望类似：

```json
{
  "part_001": {
    "oss_key": "xxx/tasks/task_xxx/part_001.mp4",
    "filename": "part_001.mp4",
    "exists": true,
    "size": 123456,
    "mime_type": "video/mp4",
    "download_url": "/v1/tasks/task_xxx/files/part_001"
  },
  "part_002": {
    "oss_key": "xxx/tasks/task_xxx/part_002.mp4",
    "filename": "part_002.mp4",
    "exists": true,
    "size": 234567,
    "mime_type": "video/mp4",
    "download_url": "/v1/tasks/task_xxx/files/part_002"
  }
}
```

---

## 3. 查看 `parts_count`

```bash
jq '.data.parts_count' /tmp/draft_detail.json
```

例如：

```json
2
```

---

## 4. 查看文件清单

```bash
jq '.data.files' /tmp/draft_detail.json
```

通常应该包含：

- `cleaned_video`
- `timeline_json`
- `transcription_json`
- `part_001`
- `part_002`
- ...

---

# 十、下载草稿产物验证

## 1. 下载 transcription_json

```bash
curl -s "$BASE_URL/v1/tasks/$DRAFT_TASK_ID/files/transcription_json" \
  -H "Authorization: Bearer $API_KEY" | tee /tmp/transcription_url.json | jq .
```

```bash
export TRANSCRIPTION_URL=$(jq -r '.data.download_url' /tmp/transcription_url.json)
curl -L "$TRANSCRIPTION_URL" -o /tmp/transcription.json
jq '.segments[:5]' /tmp/transcription.json
```

---

## 2. 下载 timeline_json

```bash
curl -s "$BASE_URL/v1/tasks/$DRAFT_TASK_ID/files/timeline_json" \
  -H "Authorization: Bearer $API_KEY" | tee /tmp/timeline_url.json | jq .
```

```bash
export TIMELINE_URL=$(jq -r '.data.download_url' /tmp/timeline_url.json)
curl -L "$TIMELINE_URL" -o /tmp/timeline.json
jq . /tmp/timeline.json | head -100
```

---

## 3. 下载 cleaned_video

```bash
curl -s "$BASE_URL/v1/tasks/$DRAFT_TASK_ID/files/cleaned_video" \
  -H "Authorization: Bearer $API_KEY" | tee /tmp/cleaned_url.json | jq .
```

```bash
export CLEANED_URL=$(jq -r '.data.download_url' /tmp/cleaned_url.json)
curl -L "$CLEANED_URL" -o /tmp/cleaned.mp4
ls -lh /tmp/cleaned.mp4
```

---

# 十一、下载切分片段文件 part

## 1. 查看所有 part key

```bash
jq '.data.parts | keys' /tmp/draft_detail.json
```

例如：

```json
[
  "part_001",
  "part_002",
  "part_003"
]
```

---

## 2. 下载单个 part 文件

```bash
curl -s "$BASE_URL/v1/tasks/$DRAFT_TASK_ID/files/part_001" \
  -H "Authorization: Bearer $API_KEY" | tee /tmp/part001_url.json | jq .
```

```bash
export PART001_URL=$(jq -r '.data.download_url' /tmp/part001_url.json)
curl -L "$PART001_URL" -o /tmp/part_001.mp4
ls -lh /tmp/part_001.mp4
```

---

## 3. 批量下载所有 part 文件

```bash
mkdir -p /tmp/draft_parts

for key in $(jq -r '.data.parts | keys[]' /tmp/draft_detail.json); do
  echo "downloading $key ..."
  url=$(curl -s "$BASE_URL/v1/tasks/$DRAFT_TASK_ID/files/$key" \
    -H "Authorization: Bearer $API_KEY" | jq -r '.data.download_url')
  curl -L "$url" -o "/tmp/draft_parts/${key}.mp4"
done
```

查看：

```bash
ls -lh /tmp/draft_parts
```

---

## 4. 对照 `editable_script.part_file` 逐段验证

```bash
jq '.data.editable_script.segments[] | {id,text,part_file,start,end,duration}' /tmp/draft_detail.json
```

重点验证：

- `id=1` 是否对应 `part_001.mp4`
- `id=2` 是否对应 `part_002.mp4`
- 每段视频内容是否与 `text` 基本一致
- `duration` 是否接近实际视频时长

---

# 十二、测试人工修稿：merge 模式

`merge` 是默认模式，按 `segment.id` 合并更新，只改你传入的字段，原有元数据保留。

---

## 1. 查看当前 editable_script

```bash
jq '.data.editable_script' /tmp/draft_detail.json
```

---

## 2. 提交 merge 更新，只修改 text

```bash
curl -s -X POST "$BASE_URL/v1/drafts/$DRAFT_TASK_ID/update" \
  -H "Authorization: Bearer $API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "replace_mode": "merge",
    "editable_script": {
      "segments": [
        {
          "id": 1,
          "text": "大家好，今天给大家正式介绍一款蓝牙耳机",
          "type": "human"
        }
      ]
    }
  }' | jq .
```

---

## 3. 再查草稿确认是否保留元数据

```bash
curl -s "$BASE_URL/v1/drafts/$DRAFT_TASK_ID" \
  -H "Authorization: Bearer $API_KEY" | jq '.data.editable_script'
```

期望结果：

- `id=1` 的 `text` 已更新
- `part_file` 还在
- `start/end/duration` 还在
- 其他未修改 segments 仍然保留

---

# 十三、测试 replace 模式

`replace` 表示完全替换当前 `editable_script`。

---

## 1. 用 replace 模式覆盖稿件

```bash
curl -s -X POST "$BASE_URL/v1/drafts/$DRAFT_TASK_ID/update" \
  -H "Authorization: Bearer $API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "replace_mode": "replace",
    "editable_script": {
      "segments": [
        {
          "id": 1,
          "text": "这是 replace 模式后的第一段",
          "type": "human",
          "scene_file": null,
          "part_file": "part_001.mp4",
          "start": 0.0,
          "end": 4.2,
          "duration": 4.2
        },
        {
          "id": 2,
          "text": "这是 replace 模式后的第二段",
          "type": "human",
          "scene_file": null,
          "part_file": "part_002.mp4",
          "start": 4.2,
          "end": 8.6,
          "duration": 4.4
        }
      ]
    }
  }' | jq .
```

---

## 2. 再次查看草稿详情

```bash
curl -s "$BASE_URL/v1/drafts/$DRAFT_TASK_ID" \
  -H "Authorization: Bearer $API_KEY" | jq '.data.editable_script'
```

期望结果：

- 当前 `editable_script` 被整份替换
- 只保留你传入的 segments
- 返回中 `replace_mode` 为 `"replace"`

---

# 十四、测试 corrections 批量替换

适合模拟“外部 AI 输出纠错对”。

假设当前文本中有这些错误词：

- `蓝牙耳际`
- `冲电`

你可以这样提交：

```bash
curl -s -X POST "$BASE_URL/v1/drafts/$DRAFT_TASK_ID/update" \
  -H "Authorization: Bearer $API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "replace_mode": "merge",
    "corrections": [
      {"src": "蓝牙耳际", "dst": "蓝牙耳机"},
      {"src": "冲电", "dst": "充电"}
    ]
  }' | jq .
```

然后再次查看：

```bash
curl -s "$BASE_URL/v1/drafts/$DRAFT_TASK_ID" \
  -H "Authorization: Bearer $API_KEY" | jq '.data'
```

重点检查：

- `editable_script` 是否被替换
- `corrections` 是否被记录下来
- `part_file/start/end/duration` 是否仍然保留

---

# 十五、测试 AI 修稿

如果已经实现 `/v1/drafts/{task_id}/ai-correct`，可以这样测试：

```bash
curl -s -X POST "$BASE_URL/v1/drafts/$DRAFT_TASK_ID/ai-correct" \
  -H "Authorization: Bearer $API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "mode": "rewrite",
    "prompt": "请修正常见错别字，保持原意不变"
  }' | jq .
```

然后查看结果：

```bash
curl -s "$BASE_URL/v1/drafts/$DRAFT_TASK_ID" \
  -H "Authorization: Bearer $API_KEY" | jq '.data'
```

重点看：

- `editable_script`
- `ai_corrections`

---

# 十六、从草稿继续合成最终视频

当草稿确认没问题后，可以发起：

```http
POST /v1/tasks/compose-from-draft
```

最小示例：

```bash
curl -s -X POST "$BASE_URL/v1/tasks/compose-from-draft" \
  -H "Authorization: Bearer $API_KEY" \
  -H "Content-Type: application/json" \
  -d "{
    \"draft_task_id\": \"$DRAFT_TASK_ID\",
    \"pipeline\": {
      \"use_transitions\": false,
      \"transition_type\": \"fade\",
      \"transition_duration\": 0.8,
      \"resync_subtitle\": true
    },
    \"asr\": {
      \"model\": \"large-v3\",
      \"device\": \"cuda\",
      \"language\": \"zh\"
    },
    \"subtitle\": {
      \"effect\": \"ad\",
      \"font_size\": 88
    },
    \"output\": {
      \"need_ass\": true
    },
    \"timeout_seconds\": 1800
  }" | tee /tmp/compose_from_draft.json | jq .
```

提取任务 ID：

```bash
export COMPOSE_TASK_ID=$(jq -r '.data.task_id' /tmp/compose_from_draft.json)
echo "$COMPOSE_TASK_ID"
```

轮询状态：

```bash
while true; do
  curl -s "$BASE_URL/v1/tasks/$COMPOSE_TASK_ID" \
    -H "Authorization: Bearer $API_KEY" | jq '.data | {task_id,status,progress,stage,error}'
  sleep 5
done
```

---

# 十七、下载最终合成结果

## 1. 下载 final_video

```bash
curl -s "$BASE_URL/v1/tasks/$COMPOSE_TASK_ID/files/final_video" \
  -H "Authorization: Bearer $API_KEY" | tee /tmp/final_video_url.json | jq .
```

```bash
export FINAL_VIDEO_URL=$(jq -r '.data.download_url' /tmp/final_video_url.json)
curl -L "$FINAL_VIDEO_URL" -o /tmp/final_video.mp4
```

---

## 2. 下载 ass_file

```bash
curl -s "$BASE_URL/v1/tasks/$COMPOSE_TASK_ID/files/ass_file" \
  -H "Authorization: Bearer $API_KEY" | tee /tmp/ass_url.json | jq .
```

```bash
export ASS_URL=$(jq -r '.data.download_url' /tmp/ass_url.json)
curl -L "$ASS_URL" -o /tmp/final.ass
```

---

## 3. 下载 resync_json

```bash
curl -s "$BASE_URL/v1/tasks/$COMPOSE_TASK_ID/files/resync_json" \
  -H "Authorization: Bearer $API_KEY" | tee /tmp/resync_url.json | jq .
```

```bash
export RESYNC_URL=$(jq -r '.data.download_url' /tmp/resync_url.json)
curl -L "$RESYNC_URL" -o /tmp/resync.json
```

---

# 十八、草稿模式测试通过标准

你可以按下面 checklist 判断。

## A. 上传链路通过
- [ ] `/v1/uploads/presign` 成功
- [ ] 文件 PUT 上传成功
- [ ] `/v1/uploads/confirm` 成功

## B. 草稿任务创建成功
- [ ] `/v1/tasks/agent-draft` 返回 `code=0`
- [ ] 得到 `task_id`

## C. 草稿任务执行成功
- [ ] `/v1/tasks/{task_id}` 最终 `status=succeeded`
- [ ] `progress=100`
- [ ] `stage=finished`

## D. 草稿数据生成成功
- [ ] `/v1/drafts/{task_id}` 返回成功
- [ ] `editable_script.segments` 不为空
- [ ] `timeline` 存在
- [ ] `transcription` 存在
- [ ] `files.cleaned_video` 存在
- [ ] `files.timeline_json` 存在
- [ ] `files.transcription_json` 存在

## E. 切片文件生成成功
- [ ] `parts` 存在
- [ ] `parts_count > 0`
- [ ] 能看到 `part_001 / part_002 / ...`
- [ ] 每个 `part_xxx` 能通过下载接口成功下载

## F. `editable_script.part_file` 正确
- [ ] 每个 segment 都带 `part_file`
- [ ] `part_file` 和真实切片文件能一一对应
- [ ] 片段视频内容和 `text` 基本一致

## G. merge 更新可用
- [ ] `/v1/drafts/{task_id}/update` 可成功调用
- [ ] `replace_mode=merge` 时仅更新指定 segment
- [ ] 原有 `part_file/start/end/duration/scene_file` 被保留
- [ ] 未提交的其他 segments 仍保留

## H. replace 更新可用
- [ ] `replace_mode=replace` 时可完整覆盖 `editable_script`
- [ ] 未传入的 segments 被移除
- [ ] 返回结果正确

## I. corrections 可应用
- [ ] 提交 `corrections` 后文本发生替换
- [ ] `corrections` 被记录
- [ ] 元数据未丢失

## J. AI 修稿可用
- [ ] `/ai-correct` 调用成功
- [ ] 返回 `ai_corrections`
- [ ] 文本被修正

## K. 草稿二次合成通过
- [ ] `/v1/tasks/compose-from-draft` 成功创建任务
- [ ] 最终状态为 `succeeded`
- [ ] 能下载 `final_video`
- [ ] 能下载 `ass_file`
- [ ] 能下载 `resync_json`

---

# 十九、推荐的人工验收动作

## 1. 抽查前 3 个 segment

```bash
jq '.data.editable_script.segments[:3]' /tmp/draft_detail.json
```

核对：

- 文本是否正常
- 时长是否合理
- `part_file` 是否存在

## 2. 播放对应 part 文件

下载：

- `part_001.mp4`
- `part_002.mp4`
- `part_003.mp4`

确认：

- 切点是否干净
- 是否有明显误切
- 口播与文本是否对应

## 3. 测试 merge 更新是否稳

只改一段 `text`，确认：

- 其他字段仍在
- 其他 segments 仍在

## 4. 测试 replace 更新是否生效

整份替换后确认：

- 旧稿被覆盖
- 新稿结构完整

## 5. 检查最终合成结果

看：

- 字幕时间轴是否对齐
- 切换是否正常
- 修稿内容是否反映到最终视频

---

# 二十、常见问题排查

## 1. 草稿任务一直不完成

先看任务详情：

```bash
curl -s "$BASE_URL/v1/tasks/$DRAFT_TASK_ID" \
  -H "Authorization: Bearer $API_KEY" | jq '.data'
```

重点看：

- `status`
- `stage`
- `error`

再看 worker 日志。

---

## 2. `editable_script` 为空

可能原因：

- 视频没有成功转写
- timeline 没生成
- 分段为空
- `build_editable_script_from_result()` 没正确映射

先检查：

```bash
jq '.data.transcription' /tmp/draft_detail.json
jq '.data.timeline' /tmp/draft_detail.json
```

---

## 3. `parts_count = 0`

可能原因：

- `cut_transition` 没生成 `part_*.mp4`
- output 目录里没有分段文件
- 上传 parts 时没有扫到文件

建议直接查 worker 输出目录逻辑或日志。

---

## 4. `part_file` 缺失

如果 `editable_script` 中没有 `part_file`，说明 segment 和切片文件的映射逻辑没有生效。  
这时需要检查草稿任务产物和 `task_runner.py` 逻辑。

---

## 5. merge 更新后字段丢失

正常情况下不应该丢。  
如果仍丢失，重点检查：

- `schemas.py` 是否已添加 `part_file`
- `api_service.py` 是否使用了合并更新逻辑
- 请求里的 `id` 是否正确

---

## 6. replace 更新后段落变少

这是正常行为。  
`replace_mode=replace` 就是整份覆盖，未传入的 segment 会被移除。

---

# 二十一、总结

草稿模式完整链路应该具备以下关键能力：

1. 上传视频
2. 生成草稿任务
3. 获得 `editable_script`
4. 每个 segment 能关联 `part_file`
5. 草稿详情返回 `parts` 和 `parts_count`
6. 可下载所有切片文件进行人工核验
7. 支持人工修稿
8. 支持 `merge` 模式安全更新
9. 支持 `replace` 模式整稿覆盖
10. 支持 corrections / AI 修稿
11. 支持从草稿继续合成最终视频

如果以上都通过，说明你的草稿模式已经具备较完整的可用性。

---
