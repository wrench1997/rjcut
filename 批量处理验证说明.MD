# 批量处理验证系统说明

## 概述

批量处理验证系统用于检查批量视频处理任务中的文件配置，确保必需文件存在，提示可选文件，并提供 JSON 查看和修改功能。

## 核心功能

### 1. 文件验证

#### 必需文件
以下文件是执行批量处理所必需的：

| 文件 | 字段名 | 格式 | 说明 | 例外情况 |
|------|--------|------|------|----------|
| 主视频 | `video_file` | MP4/MOV/AVI/MKV | 待处理的原始视频 | 无 |
| 脚本文件 | `script_file` | JSON | 包含分段和字幕信息的脚本 | `pipeline.mode="scene_only"` 时可选 |

#### 可选文件（推荐）
以下文件虽然不是必需，但强烈建议提供：

| 文件 | 字段名 | 格式 | 作用 | 推荐度 |
|------|--------|------|------|--------|
| 纠错字典 | `corrections_file` | JSON | 自动纠正常见错别字 | ⭐⭐⭐⭐ |
| 背景音乐 | `bgm_file` | MP3/WAV/M4A/AAC | 为视频添加背景音乐 | ⭐⭐⭐⭐ |
| 场景素材 | `scenes_dir` | 目录 | scene 类型片段的替换素材 | ⭐⭐⭐ |

### 2. 脚本内容验证

系统会验证脚本文件的内容是否符合规范：

```json
{
  "segments": [
    {
      "flag": "human",
      "text": "这是一个人声片段",
      "scene_file": null
    },
    {
      "flag": "scene",
      "text": "这是一个场景片段",
      "scene_file": "scenes/intro.mp4"
    }
  ]
}
```

**验证规则：**
- ✅ 必须包含 `segments` 数组
- ✅ 每个 segment 必须包含 `flag` 和 `text` 字段
- ✅ `flag` 值必须是 `human`、`scene` 或 `transition` 之一
- ✅ `scene` 类型的 segment 必须包含 `scene_file` 字段

### 3. 验证级别

系统返回三种级别的验证结果：

| 级别 | 标识 | 含义 | 处理方式 |
|------|------|------|----------|
| Error | ❌ | 错误 | 必须修复，否则无法执行 |
| Warning | ⚠️ | 警告 | 建议修复，可跳过但可能影响质量 |
| Info | ℹ️ | 提示 | 仅供参考，不影响执行 |

## 使用方法

### 方法 1：命令行验证

```bash
# 验证配置文件
python batch_validator.py ./batch_config.json

# 输出验证结果（JSON 格式）
{
  "is_valid": false,
  "total_tasks": 3,
  "valid_tasks": 2,
  "invalid_tasks": 1,
  "task_results": [...],
  "summary": {
    "total_errors": 2,
    "total_warnings": 5,
    "recommendations": [...]
  }
}
```

### 方法 2：API 验证

```bash
# 调用验证 API
curl -X POST "http://localhost:8001/v1/batch/validate" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "tasks": [
      {
        "name": "video1",
        "video_file": "./videos/video1.mp4",
        "script_file": "./scripts/video1.json"
      }
    ]
  }'
```

### 方法 3：Studio 界面验证

在 Studio 界面中使用 `BatchConfigEditor` 组件：

```jsx
import BatchConfigEditor from './components/BatchConfigEditor'

function BatchStudio() {
  const [config, setConfig] = useState({ tasks: [] })
  
  return (
    <BatchConfigEditor
      config={config}
      onChange={setConfig}
    />
  )
}
```

**界面功能：**
- ✏️ **编辑配置** - 通过表单或 JSON 编辑器修改任务配置
- 🔍 **验证配置** - 点击按钮检查所有任务
- 👁️ **预览** - 查看最终生成的 JSON 配置

## 验证报告示例

### 成功验证

```json
{
  "is_valid": true,
  "total_tasks": 2,
  "valid_tasks": 2,
  "invalid_tasks": 0,
  "task_results": [
    {
      "task_name": "video_001",
      "is_valid": true,
      "issues": [],
      "required_files": {
        "video_file": true,
        "script_file": true
      },
      "optional_files": {
        "corrections_file": true,
        "bgm_file": true,
        "scenes_dir": false
      }
    }
  ],
  "summary": {
    "total_errors": 0,
    "total_warnings": 0,
    "recommendations": []
  }
}
```

### 失败验证

```json
{
  "is_valid": false,
  "total_tasks": 1,
  "valid_tasks": 0,
  "invalid_tasks": 1,
  "task_results": [
    {
      "task_name": "video_123",
      "is_valid": false,
      "issues": [
        {
          "level": "error",
          "field": "script_file",
          "message": "缺少必需文件：脚本文件",
          "suggestion": "请提供 script_file 字段，指向有效的文件路径"
        },
        {
          "level": "error",
          "field": "script_file.segments[2]",
          "message": "第 3 个 segment 是 scene 类型但缺少 scene_file",
          "suggestion": "请提供 scene_file 字段指向场景素材"
        },
        {
          "level": "warning",
          "field": "bgm_file",
          "message": "缺少背景音乐",
          "suggestion": "建议添加背景音乐以提升视频质量"
        }
      ],
      "required_files": {
        "video_file": true,
        "script_file": false
      },
      "optional_files": {
        "corrections_file": false,
        "bgm_file": false,
        "scenes_dir": false
      }
    }
  ],
  "summary": {
    "total_errors": 2,
    "total_warnings": 1,
    "recommendations": [
      "1 个任务缺少背景音乐，建议添加以提升视频质量",
      "1 个任务缺少纠错字典，建议添加以提高字幕准确性",
      "发现 2 个错误，必须修复后才能执行批量处理"
    ]
  }
}
```

## 典型场景示例

### 场景 1：任务 123 的完整配置检查

**用户指令：**
> 检查任务 123 的配置文件

**系统响应：**
```
✅ 脚本文件：scripts/123.json (必需) ✓
✅ 数字人文件：1 分钟时长 ✓
⚠️ 转场文件：缺少，建议添加
⚠️ 背景音乐：缺少，建议添加
⚠️ 纠错字典：缺少，建议添加

AI 提示：
任务 123 的配置基本完整，但缺少以下可选文件：
- 背景音乐（建议添加以提升视频质量）
- 纠错字典（建议添加以提高字幕准确性）
- 转场素材（如果有转场需求请添加）

是否继续执行批量处理？
```

### 场景 2：批量配置自动生成

**用户指令：**
> 把 videos 目录下所有 mp4 批量生成带字幕的成片

**AI 处理流程：**
1. 扫描 `./videos/*.mp4` 获取视频列表
2. 为每个视频创建任务配置
3. 自动检查同名脚本文件
4. 验证配置完整性
5. 提示缺少的可选文件
6. 生成执行命令

**AI 响应：**
```bash
已为你生成批量配置 ✅

验证结果：
- 总任务数：5
- 有效任务：5
- 警告：5 (缺少背景音乐和纠错字典)

执行命令：
export MAX_CONCURRENT=3
export AUTO_COMPOSE=true
export BATCH_CONFIG=./batch_config.json

bash batch_process.sh

⚠️ 注意：所有任务都缺少背景音乐和纠错字典，
建议添加这些文件以提升视频质量。
是否继续执行？
```

## 配置文件模板

获取标准配置模板：

```bash
curl "http://localhost:8001/v1/batch/template" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

**模板结构：**
```json
{
  "tasks": [
    {
      "name": "task_001",
      "video_file": "./videos/video1.mp4",
      "script_file": "./scripts/video1.json",
      "corrections_file": "./corrections.json",
      "bgm_file": "./bgm.mp3",
      "scenes_dir": "./scenes",
      "custom_config": {
        "pipeline": {
          "use_transitions": false,
          "transition_duration": 0.8
        },
        "subtitle": {
          "font_size": 88,
          "effect": "ad"
        },
        "audio": {
          "bgm_volume": 0.3,
          "original_volume": 1.0
        }
      }
    }
  ],
  "_schema": {
    "required_fields": {
      "video_file": "主视频文件路径 (MP4/MOV/AVI/MKV)",
      "script_file": "脚本文件路径 (JSON 格式，除非使用 scene_only 模式)"
    },
    "optional_fields": {
      "corrections_file": "纠错字典文件路径 (JSON 格式)",
      "bgm_file": "背景音乐文件路径 (MP3/WAV/M4A/AAC)",
      "scenes_dir": "场景素材目录路径",
      "custom_config": "自定义配置对象"
    },
    "notes": [
      "script_file 在 pipeline.mode='scene_only' 时可选",
      "建议提供 corrections_file 以提高字幕准确性",
      "建议提供 bgm_file 以提升视频质量"
    ]
  }
}
```

## 错误处理

### 常见错误及解决方案

| 错误信息 | 原因 | 解决方案 |
|----------|------|----------|
| "视频文件不存在" | 文件路径错误或文件未上传 | 检查文件路径，确保文件存在 |
| "脚本 JSON 格式错误" | 脚本文件格式不正确 | 使用 JSON 验证工具检查格式 |
| "缺少必需字段：segments" | 脚本缺少 segments 数组 | 在脚本中添加 segments 字段 |
| "scene 类型缺少 scene_file" | scene 片段未指定素材 | 添加 scene_file 字段 |

### 条件性必需文件

当使用 `pipeline.mode="scene_only"` 时，脚本文件变为可选：

```json
{
  "tasks": [
    {
      "name": "scene_only_task",
      "video_file": "./videos/intro.mp4",
      "custom_config": {
        "pipeline": {
          "mode": "scene_only"
        }
      }
    }
  ]
}
```

**注意：** 纯场景模式需要提供 `input.subtitle_json_url` 参数。

## 集成指南

### 在批处理脚本中使用验证

修改 `batch_process.sh`，在执行前自动验证：

```bash
#!/bin/bash

# 验证配置
echo "🔍 验证批量配置..."
python batch_validator.py "$BATCH_CONFIG" > /tmp/validation_result.json

# 检查验证结果
is_valid=$(jq -r '.is_valid' /tmp/validation_result.json)
error_count=$(jq -r '.summary.total_errors' /tmp/validation_result.json)

if [ "$is_valid" != "true" ]; then
  echo "❌ 配置验证失败，发现 $error_count 个错误"
  jq '.task_results[] | select(.is_valid == false) | .issues[] | select(.level == "error")' /tmp/validation_result.json
  exit 1
fi

# 显示警告
warning_count=$(jq -r '.summary.total_warnings' /tmp/validation_result.json)
if [ "$warning_count" -gt 0 ]; then
  echo "⚠️ 发现 $warning_count 个警告（将继续执行）"
  jq '.summary.recommendations[]' /tmp/validation_result.json
fi

echo "✅ 配置验证通过，开始批量处理..."
# 继续执行批处理...
```

### 在 AI 助手中的使用

AI 助手应该：

1. **自动生成配置** - 根据用户需求生成 batch_config.json
2. **主动验证** - 生成配置后立即验证
3. **提示问题** - 清晰展示错误和警告
4. **提供建议** - 给出修复建议
5. **支持修改** - 允许用户通过 JSON 编辑器修改配置

## 相关文件

- `batch_validator.py` - Python 验证器核心
- `studio/src/components/BatchConfigEditor.jsx` - React 配置编辑器
- `batch_process_skill.md` - AI 技能说明文档
- `batch_process.sh` - 批处理执行脚本

## 更新日志

### v2.0 (2026-05-06)
- ✅ 新增必需文件验证
- ✅ 新增可选文件提示
- ✅ 新增脚本内容验证
- ✅ 新增 JSON 编辑器组件
- ✅ 新增 API 验证接口
- ✅ 新增 Studio 界面集成

---

**作者：** RJCut Team  
**版本：** 2.0  
**最后更新：** 2026-05-06
