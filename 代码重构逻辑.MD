短期：引入 pipeline.mode 字段，通过简单的 if/else 在 task_runner.py 和 lip_sync.py 绕过 Whisper，用纯 ffmpeg 组装画面，快速上线纯场景功能。
中期：将 task_runner.py 拆包。利用注册表模式将 agent_draft、compose、chanjing_video 拆分到不同的文件中。
长期：将音视频处理的动作（下载、ASR识别、FFMPEG切割、OSS上传）封装成独立的 Class (组件)，用责任链模式进行调用，彻底解决代码臃肿问题。