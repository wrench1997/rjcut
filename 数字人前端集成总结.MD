# 数字人前端集成总结

## 概述

本文档总结了将 RJCut 数字人 API 集成到前端 Studio 的完整过程。

## 集成内容

### 1. 新增文件

#### `studio/src/components/DigitalHumanManager.jsx`
数字人管理主组件，包含以下功能模块：

**组件结构：**
- `StatusBadge` - 状态徽章组件
- `ProgressBar` - 进度条组件  
- `DigitalPersonCard` - 数字人卡片展示组件
- `CreateVideoForm` - 创建视频生成任务表单
- `TrainPersonForm` - 训练自定义数字人表单
- `DigitalHumanManager` - 主容器组件

**功能特性：**
- ✅ 查看公共数字人列表
- ✅ 查看自定义数字人列表（数据隔离）
- ✅ 同步自定义数字人状态
- ✅ 刷新数字人详情（获取训练进度）
- ✅ 删除自定义数字人
- ✅ 创建视频生成任务
- ✅ 训练新的自定义数字人（支持上传素材视频）
- ✅ 选择声音模型（支持使用数字人原生声音）
- ✅ 配置视频参数（形象类型、驱动模式、背景等）

#### `studio/DIGITAL_HUMAN_INTEGRATION.md`
前端集成说明文档，包含使用指南和故障排除。

#### `studio/src/components/README.md`
组件说明文档，描述所有组件的功能。

---

### 2. 修改文件

#### `studio/src/api/api.js`
新增数字人 API 接口导出：

```javascript
// 获取公共数字人列表
export const getCommonPersons = () => apiClient.get('/v1/dh/persons/common');

// 获取自定义数字人列表
export const getCustomPersons = () => apiClient.get('/v1/dh/persons/custom');

// 获取自定义数字人详情
export const getCustomPersonDetail = (person_id) => apiClient.get(`/v1/dh/persons/custom/${person_id}`);

// 同步自定义数字人
export const syncCustomPersons = () => apiClient.post('/v1/dh/persons/custom/sync');

// 删除自定义数字人
export const deleteCustomPerson = (person_id) => apiClient.post(`/v1/dh/persons/custom/${person_id}/delete`);

// 获取声音列表
export const getVoices = () => apiClient.get('/v1/dh/voices');

// 删除定制声音
export const deleteVoice = (audio_id) => apiClient.post(`/v1/dh/voices/${audio_id}/delete`);

// 创建视频生成任务
export const createDhGenerateTask = (payload) => apiClient.post('/v1/dh/tasks/generate', payload);

// 创建自定义数字人训练任务
export const createDhPersonTask = (payload) => apiClient.post('/v1/dh/tasks/create-person', payload);

// 删除视频任务
export const deleteDhTask = (task_id) => apiClient.post(`/v1/dh/tasks/${task_id}/delete`);

// 删除文件
export const deleteDhFile = (file_id) => apiClient.post(`/v1/dh/files/${file_id}/delete`);
```

#### `studio/src/App.jsx`
- 导入 `DigitalHumanManager` 组件
- 新增顶部导航按钮：**🎭 数字人**
- 新增子导航标题：**数字人管理**
- 添加数字人管理页面路由

**修改的导航栏：**
```jsx
<button 
  className={`btn btn-utility ${activeTab === 'digital-human' ? 'text-primary' : ''}`}
  onClick={() => setActiveTab('digital-human')}
>
  🎭 数字人
</button>
```

**添加的路由：**
```jsx
{!vfsLoading && activeTab === 'digital-human' && (
  <div className="tile tile-light">
    <DigitalHumanManager apiKey={apiKey} />
  </div>
)}
```

---

## API 接口映射

| 前端函数 | 后端接口 | 说明 |
|---------|---------|------|
| `getCommonPersons()` | `GET /v1/dh/persons/common` | 获取公共数字人 |
| `getCustomPersons()` | `GET /v1/dh/persons/custom` | 获取自定义数字人 |
| `getCustomPersonDetail(id)` | `GET /v1/dh/persons/custom/{id}` | 获取数字人详情 |
| `syncCustomPersons()` | `POST /v1/dh/persons/custom/sync` | 同步数字人 |
| `deleteCustomPerson(id)` | `POST /v1/dh/persons/custom/{id}/delete` | 删除数字人 |
| `getVoices()` | `GET /v1/dh/voices` | 获取声音列表 |
| `deleteVoice(id)` | `POST /v1/dh/voices/{id}/delete` | 删除声音 |
| `createDhGenerateTask(payload)` | `POST /v1/dh/tasks/generate` | 创建视频任务 |
| `createDhPersonTask(payload)` | `POST /v1/dh/tasks/create-person` | 训练数字人 |
| `deleteDhTask(id)` | `POST /v1/dh/tasks/{id}/delete` | 删除视频任务 |
| `deleteDhFile(id)` | `POST /v1/dh/files/{id}/delete` | 删除文件 |

---

## 使用流程

### 查看数字人
1. 启动前端：`cd studio && npm run dev`
2. 访问 `http://localhost:5173`
3. 点击顶部导航 **🎭 数字人**
4. 查看公共数字人或自定义数字人列表

### 训练自定义数字人
1. 点击 **训练新数字人** 标签
2. 填写数字人名称
3. 上传素材视频（清晰的正面人像视频）
4. 选择训练类型（声音 + 形象 / 仅声音 / 仅形象）
5. 点击 **开始训练**
6. 等待训练完成（30 分钟 -2 小时）

### 创建视频任务
1. 在数字人列表中点击 **使用此数字人**
2. 填写要合成的文本内容
3. 选择声音模型（可选，不选则使用数字人原生声音）
4. 配置形象类型、驱动模式、背景等
5. 点击 **创建视频任务**
6. 在 **任务列表** 中查看进度

### 同步数字人状态
1. 在自定义数字人页面点击 **同步** 按钮
2. 系统会从蝉镜平台拉取最新状态
3. 更新本地数据库记录

---

## 技术细节

### API 响应处理
所有数字人 API 返回统一格式：
```json
{
  "code": 0,
  "message": "ok",
  "data": { ... }
}
```

前端通过 `response.data.code` 判断是否成功，通过 `response.data.data` 获取实际数据。

### 文件上传流程
训练数字人时的文件上传流程：
1. 调用 `presignUpload()` 获取预签名 URL
2. 使用 PUT 方法上传文件到 OSS
3. 调用 `confirmUpload()` 确认上传完成
4. 使用返回的 `oss_key` 创建训练任务

### 状态管理
- 使用 React `useState` 管理组件状态
- 使用 `useCallback` 优化回调函数
- 使用 `useEffect` 处理副作用

### 样式系统
基于 Apple 设计系统：
- 状态徽章：`status-badge` + 状态类名
- 进度条：`progress-bar` + `progress-bar-fill`
- 卡片：`card`
- 按钮：`btn` + 变体类名

---

## 状态码说明

### 数字人训练状态
| 状态码 | 说明 | 前端显示 |
|--------|------|---------|
| 10 | 训练中 | 训练中 |
| 30 | 成功 | 成功 |
| 40 | 失败 | 失败 |

### 蝉镜 API 状态（详情接口）
| 状态码 | 说明 | 映射到本地 |
|--------|------|-----------|
| 0 | 定制中 | 10 (训练中) |
| 1 | 已完成 | 30 (成功) |
| 40 | 失败 | 40 (失败) |

### 任务状态
| 状态 | 说明 |
|------|------|
| queued | 等待中 |
| processing | 处理中 |
| succeeded | 成功 |
| failed | 失败 |
| cancelled | 已取消 |

---

## 配置说明

### 环境变量
`studio/.env`:
```
VITE_API_BASE_URL=http://192.168.166.151:8001
```

### API Key
默认 API Key 已硬编码在 `api.js` 中，也可在设置页面修改。

---

## 测试方法

### 后端 API 测试
```bash
cd d:\workspace\rjcut
python test_dh_api.py
```

### 前端手动测试
1. 启动后端服务
2. 启动前端开发服务器
3. 访问数字人管理页面
4. 测试各个功能模块

---

## 注意事项

1. **跨域配置**：后端已配置 CORS 允许前端访问
2. **认证方式**：使用 Bearer Token 方式传递 API Key
3. **配额控制**：创建任务会消耗配额，请确保充足
4. **并发限制**：注意商户的并发任务数限制
5. **训练时间**：自定义数字人训练时间较长，需要耐心等待
6. **文件上传**：支持视频文件上传到 OSS

---

## 相关文件

### 前端
- `studio/src/components/DigitalHumanManager.jsx` - 数字人组件
- `studio/src/api/api.js` - API 客户端
- `studio/src/App.jsx` - 主应用
- `studio/DIGITAL_HUMAN_INTEGRATION.md` - 集成文档

### 后端
- `api_digital_human.py` - 数字人 API 路由
- `tasks/chanjing_video.py` - 任务处理器
- `chanjing_api.py` - 蝉镜 API 客户端
- `schemas.py` - 请求/响应模型
- `models.py` - 数据库模型

### 文档
- `数字人 API.MD` - API 接口文档
- `数字人前端集成总结.MD` - 本文档

---

## 后续优化建议

1. **添加数字人预览视频播放**
2. **支持批量删除数字人**
3. **添加任务进度实时推送（WebSocket）**
4. **支持更多背景选项（图片上传）**
5. **添加使用历史记录**
6. **优化大文件上传体验（断点续传）**
7. **添加数字人分类和搜索功能**

---

**集成完成日期**: 2026-05-22  
**集成版本**: v1.0  
**状态**: ✅ 已完成
