六、这个补丁的效果和限制
优点
彻底避免 ASR 错字进入最终字幕
保留重识别后的时间同步
兼容你现有字幕特效
改动相对小，不影响原有无 --script 用法
当前限制
目前字级时间是 按整段均匀分配 的，不是真正逐字强对齐。
所以：

段级同步：很好
字级动画：可用
极致精准的每个字口型点位：还不是最强
但在你这个场景里，已经比“用 Whisper 错字文本直接烧录”好很多了。

七、我再给你一个“可直接搜索定位”的改动清单
你可以按这个顺序操作：

新增函数

load_script_texts
_split_text_to_chars
build_script_aligned_json
resync_subtitle_with_script
改函数签名

compose_from_timeline(..., script_path: Optional[str] = None, ...)
改 timeline 内部逻辑

if script_path: resync_subtitle_with_script(...)
加 CLI 参数

--script
单视频模式支持 --script

用 resync_subtitle_with_script(...)
八、一个小提醒：你的 script.json 段数可能和最终识别段数不完全一致
你这个补丁里已经做了两层处理：

段数一致：直接顺序对齐
段数不一致：文本相似度顺序匹配
再不行：顺序兜底
所以一般能跑通。

但如果将来你发现某个视频脚本和识别分段差异特别大，可以再升级成：

先把脚本按标点切句
再做更细粒度匹配
目前这版先够用。